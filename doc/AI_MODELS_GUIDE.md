# AI Models Integration Guide

Руководство по AI моделям, доступным в AI Stock Bot системе, их конфигурации и использованию.

## 🤖 Доступные AI модели

### 1. Juggernaut Pro Flux (по умолчанию)
- **Model ID**: `juggernaut-pro-flux`
- **Provider**: Segmind API
- **Описание**: Профессиональные реалистичные изображения с отличной детализацией
- **Формат вывода**: Binary buffer (прямая обработка)
- **Разрешение**: 1024x1024 (обрабатывается до 4000x4000)
- **Конфигурация**: Требует `SEGMIND_API_KEY`
- **Особенности**: Оптимизирован для стоковых изображений

### 2. OpenAI DALL-E 3
- **Model ID**: `dall-e-3`
- **Provider**: OpenAI API
- **Описание**: Высококачественная генерация с отличным пониманием промптов
- **Формат вывода**: URL (загружается и обрабатывается)
- **Разрешение**: 1024x1024 (обрабатывается до 4000x4000)
- **Конфигурация**: Требует `OPENAI_API_KEY`
- **Особенности**: Content filtering, revised prompts, премиум качество

### 3. Seedream V3
- **Model ID**: `seedream-v3`
- **Provider**: Segmind API
- **Описание**: Художественная и креативная генерация с уникальным стилем
- **Формат вывода**: Binary buffer (прямая обработка)
- **Разрешение**: 1024x1024 (обрабатывается до 4000x4000)
- **Конфигурация**: Требует `SEGMIND_API_KEY`
- **Особенности**: Идеален для арт-контента и креативных изображений

### 4. HiDream-I1 Fast
- **Model ID**: `hidream-i1-fast`
- **Provider**: Segmind API
- **Описание**: Быстрая высококачественная генерация, оптимизированная для скорости
- **Формат вывода**: Binary buffer (прямая обработка)
- **Разрешение**: 1024x1024 (обрабатывается до 4000x4000)
- **Конфигурация**: Требует `SEGMIND_API_KEY`
- **Особенности**: Очень быстрая обработка для высокой нагрузки

## 🔧 Конфигурация

### Переменные окружения

```bash
# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TIMEOUT=60000

# Segmind Configuration
SEGMIND_API_KEY=your_segmind_api_key_here
SEGMIND_BASE_URL=https://api.segmind.com/v1
SEGMIND_TIMEOUT=120000
```

### Конфигурация базы данных

Система использует динамическую конфигурацию, хранящуюся в MongoDB. Конфигурация AI моделей автоматически инициализируется со всеми доступными моделями при запуске системы.

Конфигурация по умолчанию:
```json
{
  "activeModel": "juggernaut-pro-flux",
  "models": {
    "juggernaut-pro-flux": {
      "enabled": true,
      "provider": "segmind",
      "description": "Juggernaut Pro Flux - Professional quality realistic images"
    },
    "hidream-i1-fast": {
      "enabled": true,
      "provider": "segmind",
      "description": "HiDream-I1 Fast - Quick high-quality image generation"
    },
    "seedream-v3": {
      "enabled": true,
      "provider": "segmind",
      "description": "Seedream V3 - Artistic and creative image generation"
    },
    "dall-e-3": {
      "enabled": true,
      "provider": "openai",
      "description": "OpenAI DALL-E 3 - High quality image generation"
    }
  }
}
```

## 🔄 Каскадная система фоллбеков

Система реализует сложный каскадный механизм фоллбеков, который автоматически пробует несколько AI моделей в порядке приоритета:

### Порядок фоллбеков
1. **Juggernaut Pro Flux** (Основная - модель по умолчанию)
2. **HiDream-I1 Fast** (Первый фоллбек)
3. **Seedream V3** (Второй фоллбек)
4. **DALL-E 3** (Третий фоллбек)
5. **Mock Images** (Финальный фоллбек)

### Как это работает
- При запросе генерации система начинает с активной модели
- Если активная модель не работает, автоматически пробует следующую в цепочке
- Каждая попытка логируется с детальной информацией об ошибке
- Только если все AI модели не работают, система переходит на mock изображения
- Пользователь получает изображение от первой успешной модели

### Триггеры фоллбека
- Ошибки аутентификации API (401, 403)
- Rate limiting/превышение квоты (429)
- Серверные ошибки (5xx)
- Таймауты сети
- Неверные ответы API

### Конфигурация
Порядок фоллбеков настраивается в `backend/src/config/config.js`:
```javascript
aiModels: {
  fallbackOrder: [
    'juggernaut-pro-flux',
    'hidream-i1-fast', 
    'seedream-v3',
    'dall-e-3'
  ]
}
```

## 🔀 Переключение AI моделей

### Через Admin API

```bash
# Переключение на Juggernaut Pro Flux
POST /api/admin/config/ai-model/switch
{
  "model": "juggernaut-pro-flux",
  "reason": "Testing new model"
}

# Переключение на Seedream V3
POST /api/admin/config/ai-model/switch
{
  "model": "seedream-v3",
  "reason": "Artistic generation needed"
}

# Переключение на HiDream-I1 Fast
POST /api/admin/config/ai-model/switch
{
  "model": "hidream-i1-fast",
  "reason": "Fast generation required"
}

# Переключение на DALL-E 3
POST /api/admin/config/ai-model/switch
{
  "model": "dall-e-3",
  "reason": "High quality generation"
}
```

### Через Configuration Service

```javascript
const configService = require('./src/services/configService');

// Программное переключение модели
await configService.switchAIModel('juggernaut-pro-flux', 'admin', 'Testing new model');
```

## 🔄 Pipeline обработки изображений

Все AI модели следуют одному pipeline обработки:

1. **Генерация**: AI модель генерирует изображение в своем нативном формате
2. **Обработка формата**: 
   - URL формат: Загрузка изображения
   - Base64 формат: Конвертация в buffer
   - Buffer формат: Использование напрямую
3. **Обработка**: Изменение размера до 4000x4000, оптимизация качества, добавление EXIF данных
4. **Хранение**: Сохранение в директорию `backend/temp/`
5. **База данных**: Сохранение метаданных и информации о файле

## 🎯 Особенности моделей

### OpenAI DALL-E 3
- Фильтрация контента и безопасность
- Генерация пересмотренных промптов
- Высокое понимание промптов
- Множественные опции качества/стиля

### Segmind модели
- Прямой binary вывод (быстрая обработка)
- Консистентное соотношение сторон 1:1
- Профессиональное качество, подходящее для стоковой фотографии
- Меньшая задержка по сравнению с OpenAI

## 🛡️ Механизм фоллбека

Если активная AI модель не работает, система автоматически:

1. Логирует ошибку с детальной информацией
2. Генерирует fallback изображение с контекстом ошибки
3. Продолжает обработку без прерывания пользовательского опыта
4. Предоставляет детальную информацию об ошибке для отладки

## 📊 Мониторинг и логирование

Система предоставляет comprehensive логирование для:

- События переключения моделей
- Запросы и ответы генерации
- Обработка ошибок и фоллбеки
- Метрики производительности
- Изменения конфигурации

## 📋 API Endpoints

### Получение текущей конфигурации
```bash
GET /api/admin/config/ai-models
```

### Получение доступных моделей
```bash
GET /api/admin/config/ai-models/available
```

### Переключение активной модели
```bash
POST /api/admin/config/ai-model/switch
{
  "model": "model-id",
  "reason": "Optional reason"
}
```

### Получение истории конфигурации
```bash
GET /api/admin/config/ai-models/history
```

Подробная документация: [ADMIN_API_GUIDE.md](ADMIN_API_GUIDE.md)

## ✅ Best Practices

1. **Управление API ключами**: Храните API ключи безопасно в переменных окружения
2. **Выбор модели**: Выбирайте модели на основе случая использования:
   - DALL-E 3: Лучшее общее качество и безопасность
   - Juggernaut Pro Flux: Профессиональные реалистичные изображения
   - Seedream V3: Художественный и креативный контент
   - HiDream-I1 Fast: Быстрая генерация
3. **Мониторинг**: Отслеживайте успешность генерации и переключайте модели при необходимости
4. **Фоллбек**: Всегда имейте механизмы фоллбека
5. **Тестирование**: Тестируйте новые модели в development перед переключением в production

## 🔧 Устранение неполадок

### Частые проблемы

1. **Неверный API ключ**: Проверьте переменные окружения и валидность API ключа
2. **Превышение квоты**: Мониторьте использование API и реализуйте rate limiting
3. **Таймауты сети**: Настройте таймауты для медленных моделей
4. **Ошибки обработки изображений**: Проверьте установку Sharp библиотеки и права на temp директорию

### Debug информация

Включите детальное логирование установкой:
```bash
LOG_LEVEL=debug
```

Это предоставит детальную информацию о:
- API запросах и ответах
- Шагах обработки изображений
- Деталях ошибок и stack traces
- Метриках производительности

## 🚀 Производительность

### Сравнение моделей по скорости:
1. **HiDream-I1 Fast** - ~5-10 секунд
2. **Juggernaut Pro Flux** - ~10-15 секунд
3. **Seedream V3** - ~10-20 секунд
4. **DALL-E 3** - ~15-30 секунд

### Оптимизации:
- Segmind модели используют прямой binary вывод
- OpenAI модели требуют дополнительной загрузки по URL
- Кэширование конфигурации для быстрого доступа
- Потоковая передача изображений без буферизации

## 🔮 Планы развития

### Планируемые модели:
- **Stable Diffusion XL** - через Segmind API
- **Midjourney** - через API (когда станет доступен)
- **Adobe Firefly** - корпоративная интеграция
- **Custom Models** - поддержка пользовательских моделей

### Улучшения системы:
- A/B тестирование моделей
- Автоматический выбор модели на основе промпта
- Балансировка нагрузки между провайдерами
- Оптимизация стоимости на основе использования
